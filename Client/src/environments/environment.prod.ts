export const environment = {
  production: true,
  apiurl: window.location.href.replace(window.location.href .substring(window.location.href.indexOf(window.location.pathname),window.location.href.length), "")

};
